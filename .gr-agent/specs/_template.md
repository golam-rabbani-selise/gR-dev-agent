# <Feature name>

> Status: draft

## Summary
What and why, in 2-3 lines.

## Affected areas
Seam(s) touched (`src/platform/`, `src/orchestrator/`, `src/workers/`, `src/llm/`,
`src/integrations/providers/`, `src/ui/`, `src/cli/`), files/modules likely to change.

## Contract (source of truth once approved)
Public API / CLI surface touched: exported functions/types, CLI flags/commands, config shape,
`integrations.manifest.json` entries. This section is locked once status moves to `approved` —
changing it later means updating the spec first, not the code first.

## Implementation checklist
- [ ] ...

## Risks / open questions
Anything needing a human decision.
